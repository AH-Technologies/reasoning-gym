# Core dependencies for GRPO training with Reasoning Gym + Verifiers
# Install with: pip install -r requirements.txt

# Reasoning Gym - procedural dataset generators
reasoning-gym>=0.1.14

# Verifiers - GRPO training framework
verifiers[train]

# Deep learning frameworks
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
datasets>=2.14.0

# vLLM for efficient inference
vllm>=0.2.0

# Flash Attention (install separately after other packages)
# Run: pip install flash-attn --no-build-isolation

# Optimization
bitsandbytes>=0.41.0

# Logging and monitoring (optional)
wandb>=0.15.0
tensorboard>=2.14.0

# Utilities
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0